% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gef.R
\name{gef_to_h5ad}
\alias{gef_to_h5ad}
\title{Convert GEF file directly to H5AD format}
\usage{
gef_to_h5ad(
  gef_file,
  h5ad_file,
  bin_type = c("cell_bins", "bins"),
  layer = c("counts", "data"),
  max_cells = NULL,
  region = NULL,
  gene_list = NULL,
  include_cellborder = TRUE,
  include_spatial = TRUE,
  overwrite = FALSE,
  name_conflict = c("make_unique", "error"),
  verbose = TRUE
)
}
\arguments{
\item{gef_file}{Path to the GEF file}

\item{h5ad_file}{Output H5AD file path}

\item{bin_type}{Type of binning ("cell_bins" or "bins", default "cell_bins")}

\item{layer}{Which layer to use as main X matrix ("counts" or "data", default "counts")}

\item{max_cells}{Maximum number of cells to read (for memory management)}

\item{region}{Spatial region filter c(minX, maxX, minY, maxY)}

\item{gene_list}{Optional vector of genes to filter}

\item{include_cellborder}{Whether to include cell borders (default TRUE)}

\item{include_spatial}{Whether to include spatial coordinates (default TRUE)}

\item{overwrite}{Whether to overwrite existing H5AD file (default FALSE)}

\item{name_conflict}{Strategy for handling duplicate names ("make_unique" or "error")}

\item{verbose}{Whether to print progress messages (default TRUE)}
}
\value{
Invisibly returns the output file path
}
\description{
This function reads a Stereo-seq GEF file and directly converts it to H5AD format
without creating intermediate R objects. This is memory efficient for large datasets.
}
\examples{
\dontrun{
# Basic conversion
gef_to_h5ad("sample.cellbin.gef", "output.h5ad")

# With filtering and custom settings
gef_to_h5ad(
  gef_file = "large_sample.cellbin.gef",
  h5ad_file = "filtered.h5ad", 
  max_cells = 5000,
  region = c(1000, 5000, 1000, 5000),
  overwrite = TRUE
)
}

}
